<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Cantos arredondados -->
    <corners android:radius="12dp" />

    <!-- Cor de fundo padrão (Pendente) -->
    <solid android:color="@color/cinza_claro_chip" />

    <!-- Borda sutil -->
    <stroke
        android:width="1dp"
        android:color="@color/cinza_borda_chip" />

    <!-- Margens internas -->
    <padding
        android:left="8dp"
        android:right="8dp"
        android:top="4dp"
        android:bottom="4dp" />
</shape>
